<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title> <!-- заменил Document на {{ title }}--> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" type="text/css" href="/style.css?v=1">

    <style>
        /* добавил стиль */
        a.active {
            color: #3D68ED;
        }
        .fa-brands.fa-squarespace,
        .nav-link.header.active {
            color: #3D68ED;
        }    
        .history_block {
            width: 500px;
            padding: 0;
        }
        .history {
            padding: 10px 25px;
            margin: 10px 0;
            text-wrap: wrap;
            background-color: #e0eaff;
            list-style: none;
            border-radius: 5px;
            overflow: hidden;
            white-space: nowrap;
            text-overflow: ellipsis;
        }
        .history > a {
            color: #1f1f1f;
        }
   
    </style>    
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
        <a class="navbar-brand" href="#"><i class="fa-solid fa-gamepad fa-xl"></i></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <a class="nav-link" href="/">Главная</a>
                    {% for type in types %}
                        <li class="nav-item">
                            <a class="nav-link" href="/?type={{ type.name }}">{{ type.name }}</a>
                        </li>
                    {% endfor %}
                    <li class="nav-item">
                        <a class="nav-link" href="/search">Поиск</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/create">Добавить</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/createType">Добавить тип</a>
                    </li>
                    <form action="/set-welcome/">
                        <input type="text" name="message" class="form-control mr-2 ml-2">
                    </form>

                    <li calss= "nav-item">
                        <form action="/logout">
                            <button type="submit" class="btn btn-primary">Выйти</button>
                        </form>
                    </li>
                </ul>
                 
            </div>
            
        </div>
    </nav>
    </nav>

    




    <div class='container'>
        <div class="row">
            <div class="col-9">
                {% block content %} 
                 
                {% endblock %}
            </div>

            <div class="col-3">
                <h5>История посещений:</h5>
                <ul class='history_block'>
                    {% for page in history %}
                    <li class='history'>
                        <a href='{{page}}'>{{ page }}</a>
                    </li>
                    {% endfor %}
                </ul>
            </div>
           
        </div>
    </div>
</body>
</html>



